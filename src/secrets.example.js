// Create a file named secrets.js with your API KEY

const API_KEY = 'Your key here';

// This works for me so no one can see my apikey in the GitHub repo.
// This doesn't actually prevent hiding it in practice.